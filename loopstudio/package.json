{
  "name": "mini_projects",
  "version": "1.0.0",
  "description": "tailwindcss-starter",
  "main": "tailwind.config.js",
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./css/style.css",
    "watch": "tailwindcss -i ./input.css -o ./css/style.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Cyrus",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.3.2"
  }
}